{
  font-family: "Alumni Sans SC", sans-serif;
  font-optical-sizing: auto;
  font-weight: <weight>;
  font-style: normal;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    text-decoration: none;
    list-style: none;
}

:root {
    --roxoprincipal: #7434d4; 
    --roxofundoclaro: #f0e4fc;
    --roxobotao: #a864fc;
    --roxotexto: #401c74;
}

body {
    font-family: "Alumni Sans SC", sans-serif;
    background-color: var(--roxofundoclaro);
    color: var(--roxotexto);
}

.background-roxo {
    background: linear-gradient(to right, var(--roxofundoclaro) 55%, var(--roxoprincipal) 45%);
    min-height: 100vh;
    width: 100%;
    display: flex;
    flex-direction: column;
}

.container {
    max-width: 1440px;
    padding: 0 5%;
    margin: 0 auto;
}

nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 30px 0;
}

.logo a {
    font-size: 28px;
    font-weight: bold;
    color: var(--roxotexto);
}

.ul {
    display: flex;
    gap: 30px;
}

.ul li a {
    color: white;
    font-weight: bold;
    font-size: 14px;
    transition: color 0.3s;
}

.ul li a:hover {
    color: var(--roxobotao);
}

.hero {
    flex: 1;
    display: flex;
    align-items: center;
    padding: 50px 0;
}

.hero-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
}


.hero-left {
    width: 100%;
    max-width: 600px;
}

.hero-left h1 {
    font-size: 48px;
    color: var(--roxotexto);
    margin-bottom: 20px;
}

.hero-left p {
    font-size: 18px;
    color: #5b3a7a;
    margin-bottom: 30px;
    max-width: 450px;
}

.botao-roxo {
    display: inline-block;
    padding: 15px 30px;
    background-color: var(--roxobotao);
    color: white;
    border-radius: 8px;
    font-weight: bold;
    font-size: 16px;
    margin-bottom: 25px;
}
